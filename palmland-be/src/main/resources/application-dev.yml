spring:
  jpa:
    hibernate:
      ddl-auto: update
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    properties:
      hibernate:
        dialect=org:
          hibernate:
            dialect: org.hibernate.dialect.MariaDB103Dialect
  datasource:
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    url: jdbc:mysql://localhost:3306/palmland_db?allowPublicKeyRetrieval=true&useSSL=false
